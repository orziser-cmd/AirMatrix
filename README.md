<div dir="rtl">

# הצעת הפרויקט: מערכת ניהול הזמנות וכרטוס טיסות (AirMatrix) ✈️
מגישות - אור זיסר | עדן ביטון | אור מנשה | נועה רשטי שקורי

---

### 📌 1. שם הפרויקט ותיאור המערכת 
> **AirMatrix System** – מערכת ממוחשבת לניהול מלאי טיסות, מטריצת מושבים וזמינותם, כרטוס וחישוב תעריפי כבודה ומחלקות שירות בזמן אמת.

---

### 🎯 2. הצורך העסקי
חברות תעופה וסוכנויות נסיעות מתמודדות עם ניהול מלאי מושבים דינמי, מניעת הזמנות יתר (Overbooking) כפולות על אותו מושב, ותמחור מורכב המשתנה לפי מחלקת השירות ומדיניות הכבודה. בהיעדר מערכת מסודרת, עלולים להיווצר שיבושי הושבה, תורי המתנה לא מנוהלים בשער העלייה למטוס (Boarding), וחישובי מחיר שגויים בעת הוספת כבודה. המערכת מספקת תשתית אמינה לניהול תהליך ההזמנה, נעילת מושבים זמנית בעת תשלום, ותיעדוף עלייה למטוס בהתאם למדיניות חברת התעופה.

---

### 👥 3. המשתמשים המרכזיים ותפקידם

| משתמש | תפקיד ואחריות במערכת |
| :--- | :--- |
| **נוסע (Passenger)** | מחפש טיסות זמינות, בוחר מחלקת שירות (תיירים / עסקים), בוחר מושב פנוי, מוסיף כבודה ומבצע הזמנה. |
| **דייל קרקע / נציג שירות (Check-in Agent)** | מאמת את פרטי הנוסע, בודק את משקל הכבודה בפועל, מאשר את שיבוץ המושב הסופי ומנהל את תור העלייה למטוס. |
| **מנהל טיסות ותפעול (Flight Operations Manager)** | פותח טיסות חדשות, מגדיר את תצורת המטוס והמושבים, קובע מחירי בסיס ומפיק דוחות תפוסה והכנסות לכל טיסה. |

---

### 🔄 4. התהליך העסקי המרכזי

* **מה מפעיל אותו:** בחירת טיסה, מחלקת שירות ומושב על ידי נוסע לצורך ביצוע הזמנה.
* **מהלך התהליך:**
  1. המערכת בודקת שפורמט המושב חוקי ושהמושב במצב פנוי (Available), ונועלת אותו זמנית במצב המתנה לתשלום (Pending Payment) למניעת תפיסה מקבילה.
  2. מתבצעת בדיקת תקינות לפרטי הנוסע והכבודה, ומחושב המחיר הסופי בהתאם לסוג המחלקה (תוספות כבודה באקונומי מול הטבות כלולות בביזנס).
  3. לאחר אישור התשלום, המערכת מנפיקה כרטיס טיסה רשמי, משנה את סטטוס המושב לתפוס (Occupied), ומשבצת את הנוסע לתור העלייה למטוס בהתאם לרמת הקדימות שלו.
* **התוצאה:** מונפק כרטיס עלייה למטוס, המושב מסומן כתפוס במלאי, וההכנסה נרשמת בדוח הטיסה.

---

### 📊 5. זרימת המידע

* **מידע נכנס:** פרטי הטיסה (מספר טיסה, מוצא, יעד), פרטי הנוסע (שם, דרכון), בחירת מחלקה (תיירים / עסקים), מספר מושב מבוקש ומשקל כבודה.
* **עדכון המידע:** המערכת מעדכנת את זמינות המושבים (פנוי, ממתין לתשלום, תפוס), ודיילי הקרקע מעדכנים סטטוס עלייה למטוס ומשקל כבודה סופי.
* **מידע מופק:** כרטיסי עלייה למטוס, תורי עלייה למטוס מדורגים לפי קדימות, רשימות המתנה (Standby) במקרה של ביטולים, ודוחות הכנסות ותפוסה למנהלי התפעול.

---

### 💼 6. הערך העסקי הצפוי
מניעת תקלות של שיבוץ כפול (Double Booking), ייעול וקיצור זמני העלייה למטוס באמצעות תעדוף מובנה, מקסום הכנסות מכבודה נלווית ושדרוגי מחלקות, ושיפור חוויית הלקוח באמצעות תהליך הזמנה שקוף ומהיר.

---

### 🧩 7. עקרונות תכנות מונחה-עצמים (OOP)
* **הרכבה (Composition):** מחלקת `Ticket` מכילה בתוכה אובייקט מסוג `Passenger`. הקשר הוא "יש לו" (Has-a) – כרטיס מחזיק נוסע, מה שמפריד את פרטי הנוסע (שם, דרכון) מנתוני הטיסה והתמחור.
* **מחלקה אבסטרקטית (Abstract Base Class):** מחלקת הבסיס `Ticket` יורשת מ-`abc.ABC` ומגדירה מתודה מופשטת `@abstractmethod def calculate_final_price(self)`. לא ניתן לייצר מופע ישיר של `Ticket`, והיא מחייבת כל תת-מחלקה לממש חישוב מחיר סופי.
* **הורשה (Inheritance):** המחלקות `EconomyTicket` ו-`BusinessTicket` יורשות ממחלקת הבסיס `Ticket` את כל שדות הבסיס (מספר כרטיס, טיסה, נוסע, מחיר בסיס).
* **פולימורפיזם (Polymorphism):** כל מחלקה נגזרת מממשת את המתודה `calculate_final_price()` באופן שונה – `EconomyTicket` מוסיפה חיוב לפי משקל כבודה עודף מעבר למותר, בעוד ש-`BusinessTicket` כוללת שתי מזוודות ללא עלות ומוסיפה עמלת שירותי פרימיום. פונקציות העיבוד קוראות לאותה מתודה ומקבלות התנהגות מותאמת מחלקה ללא תנאי `if/else`.

---

### 💡 8. שני תרחישי שימוש מרכזיים (Use Cases)

#### תרחיש 1 – הזמנת כרטיס עסקים וחישוב כבודה
> נוסע מזמין כרטיס במחלקת ביזנס עם 2 מזוודות. המערכת מזהה את סוג הכרטיס, משבצת אותו במושב קדמי, מחשבת מחיר הכולל את מלוא הכבודה ללא חיוב חורג, ומשבצת אותו אוטומטית בעדיפות עליונה בתור העלייה למטוס.

#### תרחיש 2 – ניהול רשימת המתנה (Standby) למושב שמתפנה
> הטיסה מלאה לחלוטין ונוסע מבקש להירשם ברשימת המתנה. המערכת מוסיפה אותו לתור המתנה בשיטת FIFO (כל הקודם זוכה). כאשר נוסע קיים מבטל את הזמנתו ומושב מתפנה, המערכת שולפת אוטומטית את הנוסע הראשון ברשימת ההמתנה ומאפשרת לו להשלים את הרכישה.

---

### 🚀 9. רעיון להרחבת המערכת בשלב עתידי
חיבור המערכת לשירות API חיצוני של לוחות טיסות ומזג אוויר לקבלת עדכוני זמני המראה ועיכובים בזמן אמת, לצד הרצת תהליכי מקביליות לעיבוד מהיר של מאות בקשות צ'ק-אין מקוונות במקביל לפני סגירת טיסה.

---

### 🗂️ 10. מפת הקבצים ומבנה הפרויקט בפועל

* **`AirMatrix/__init__.py`:** קובץ חבילה (Package) המגדיר את התיקייה כמודול פייתון.
* **`AirMatrix/models.py`:** מחלקות עסקיות, הרכבה, הורשה, פולימורפיזם ואימות נתונים (Validation).
* **`AirMatrix/repository.py`:** קריאת קובץ הנתונים שורה-אחר-שורה ב-`with open`, המרת רשומות JSON למילונים ואיתור/יצירת אובייקטי כרטיס מתאימים באמצעות מחולל (`yield`).
* **`AirMatrix/processing.py`:** עיבוד אוספים, שימוש ב-Comprehensions ומיון כרטיסים.
* **`AirMatrix/iterators.py`:** מימוש של Iterable, Iterator, Generators וצינור עיבוד (Pipeline) עצל.
* **`AirMatrix/context_managers.py`:** מימוש Context Manager מותאם אישית (מימוש `__enter__` ו-`__exit__`).
* **`AirMatrix/main.py`:** קובץ הרצה המדגים את פעולת המערכת, תרחישי השימוש ובדיקות השגיאות.
* **`data/sample_data.jsonl`:** מאגר הנתונים המדומה (אובייקט JSON אחד בכל שורה).
* **`AI_USAGE.md`:** תיעוד השימוש בכלי בינה מלאכותית, פרומפטים, אימות ותיקונים.
* **`pyproject.toml`:** קובץ תצורת הפרויקט והגדרת גרסת פייתון.
* **`.gitignore`:** הגדרת סינון קבצים שלא ייכנסו לבקרת הגרסאות.

---

### 🗃️ 11. טבלת מבני הנתונים

| צורך במערכת | מבנה שנבחר | מדוע הוא מתאים |
| :--- | :--- | :--- |
| **אחסון וניהול כרטיסים לפי קוד כרטיס** | `dict` | מאפשר גישה, שליפה ועדכון של כרטיס ספציפי בסיבוכיות זמן של $O(1)$ ממוצע לפי מפתח ייחודי (`ticket_id`). |
| **מעקב אחר קווי טיסה ייחודיים (Routes)** | `set` | מונע כפילויות באופן מובנה ומאפשר בדיקת שייכות וקיום קו ב-$O(1)$. |
| **אוסף כרטיסים למיון ולתצוגה מדורגת** | `list` | שומר על סדר האיברים, תומך באלגוריתמי מיון מובנים ואיטרציה סדרתית מהירה. |
| **הגדרת קבוצות מחלקות ומאפיינים קבועים** | `tuple` | שמירה על נתונים סטטיים בלתי ניתנים לשינוי (Immutable) עם צריכת זיכרון מינימלית. |

---

### 📥 12. מקור הנתונים, מבנה הקובץ ותהליך הטעינה
* **מקור הנתונים:** קובץ נתונים סינתטי שנוצר באמצעות AI ומדמה הזמנות טיסה אמיתיות.
* **מבנה הקובץ (`data/sample_data.jsonl`):** קובץ בפורמט JSON Lines – כל שורה בו היא אובייקט JSON עצמאי הכולל שדות כגון: `ticket_id`, `flight_number`, `origin`, `destination`, `base_price`, `seat_class`, ופרטי `passenger`.
* **תהליך הטעינה ל-dict וההמרה לאובייקטים:**
  1. קריאת הקובץ שורה אחר שורה באמצעות Generator ב-`TicketRepository` (בשיטת Streaming).
  2. פענוח כל שורה למילון (`dict`) באמצעות `json.loads`.
  3. ולידציה של טיפוסי הנתונים ושדות החובה.
  4. יצירת אובייקט `Passenger` מתוך נתוני הנוסע.
  5. יצירת מופע של המחלקה המתאימה (`EconomyTicket` או `BusinessTicket`) לפי ערך השדה `seat_class` והזרקת אובייקט הנוסע לתוכו (Composition).

---

### 🔄 13. טבלת פעולות שמשנות אוסף לעומת פעולות שיוצרות אוסף חדש

| פעולה | סוג הפעולה | הסבר והשפעה על הזיכרון |
| :--- | :--- | :--- |
| `tickets.sort(key=...)` | **משנה את האוסף הקיים (In-Place)** | ממיינת ישירות את הרשימה הקיימת בזיכרון, אינה מייצרת עותק חדש ואינה צורכת זיכרון נוסף ($O(1)$ נוסף). |
| `sorted(tickets, key=...)` / List Comprehension | **יוצרת אוסף חדש** | יוצרת ומחזירה רשימה חדשה לגמרי בזיכרון. הרשימה המקורית נשארת ללא שינוי, אך נדרשת הקצאת זיכרון נוספת בגודל $O(n)$. |

---

### 🔁 14. מימוש Iterator ו-Iterable
בפרויקט מימשנו ב-`AirMatrix/iterators.py` מחלקת איטרטור ייעודית למעבר על כרטיסים:
* **`__iter__()`:** מתודה שהופכת את האובייקט ל-Iterable ומחזירה את מופע האיטרטור (`self`).
* **`__next__()`:** מתודה המחזירה את הכרטיס הבא באוסף בכל קריאה. כאשר מוצו כל הכרטיסים, המתודה זורקת חריגת `StopIteration`, המאפשרת ללולאות `for` לסיים את הריצה בצורה תקינה.

---

### ⚙️ 15. הסבר על ה-Generator וה-Pipeline העצל (חלק ד', סעיף 3)

בפרויקט מימשנו צינור עיבוד עצל (`run_lazy_ticket_pipeline`) בעל שלושה שלבים עוקבים באמצעות Generator Expressions:
1. **שלב 1 (סינון ראשוני):** סינון כרטיסים שעלות הבסיס שלהם היא 500 ומעלה (`base_price >= 500`).
2. **שלב 2 (המרה וחישוב):** חישוב המחיר הסופי (`calculate_final_price()`) ושליפת מזהה הכרטיס עבור הכרטיסים שעברו את הסינון.
3. **שלב 3 (הפקת שדה מסכם):** יצירת מחרוזת תמציתית המציגה את מזהה הכרטיס לצד מחירו הסופי.

בצינור זה לא נוצרות רשימות ביניים כלל, והוא מבוסס על משיכה עצלה (Pull) בלבד:

* **מה גורם ל-Pipeline להתחיל לעבוד:**
  הקריאה היזומה לפונקציה `next()` (או שימוש בלולאת `for` הצורכת פריטים). עד לרגע הקריאה, הג'נרטורים נמצאים במצב "עצל" ואינם מבצעים שום פעולה. ברגע שמתבצעת קריאה ל-`next()`, שלב 3 "מושך" נתון משלב 2, שמושך נתון משלב 1, שמאתר את הכרטיס הראשון שעומד בתנאי.

* **אילו פריטים לא היה צורך לעבד לאחר העצירה:**
  מכיוון שצרכנו רק את שתי התוצאות הראשונות באמצעות שתי קריאות `next()` ועצרנו, כל שאר הכרטיסים שנמצאים באוסף אחרי הכרטיס השני שנשלף לא עובדו כלל. תנאי הסינון של שלב 1 מעולם לא נבדק לגביהם, וחישוב המחיר הסופי של שלב 2 מעולם לא הופעל עליהם, מה שחסך זמן ריצה ומשאבי חישוב.

* **מה ההבדל בין list comprehension לבין Generator Expression במקרה שלנו:**
  * **ב-List Comprehension (סוגריים מרובעים `[...]`):** פייתון הייתה מעבדת באופן מיידי את כל הכרטיסים באוסף, מייצרת שלוש רשימות ביניים מלאות בזיכרון המחשב, ומבזבזת זיכרון וזמן עיבוד עוד לפני שהחלטנו כמה פריטים נרצה לקרוא.
  * **ב-Generator Expression (סוגריים עגולים `(...)`):** לא נוצרת אף רשימה בזיכרון. הנתונים מחושבים ומועברים אחד-אחד רק בזמן הדרישה בפועל, תוך שימוש מינימלי וקבוע בזיכרון ($O(1)$ זיכרון נוסף).

* **מדוע יש ליצור Generator חדש כדי לבצע מעבר נוסף מההתחלה:**
  ג'נרטור בפייתון הוא מבנה נתונים חד-כיווני וחד-פעמי (Exhaustible). הוא אינו שומר את כל ההיסטוריה בזיכרון ואינו מחזיק מצביע שניתן להחזירו אחורה (Reset). ברגע שג'נרטור התקדם או סיים את ריצתו, מצבו אינו ניתן לאיפוס, וכדי להתחיל סריקה חדשה מההתחלה חובה ליצור מופע חדש של הג'נרטור.

---

### 🛡️ 16. ה-Context Manager ומה קורה בו במקרה של חריגה
מומש Context Manager ייעודי בשם `TicketPriceModifier` (ב-`AirMatrix/context_managers.py`):
* **כניסה (`__enter__`):** מחיל הנחה/תוספת זמנית על מחירי הכרטיסים עבור בלוק הרצה מוגדר.
* **יציאה תקינה (`__exit__`):** משחזר את מחירי הבסיס של הכרטיסים למצבם המקורי בסיום הבלוק.
* **טיפול בחריגה (Exception):** אם נזרקת שגיאה בתוך הבלוק, מתודת `__exit__` מקבלת את פרטי החריגה (`exc_type`, `exc_val`, `exc_tb`). המתודה משחזרת באופן מובטח את מחירי הכרטיסים לקדמותם כדי למנוע השחתת נתונים, רושמת הודעת שגיאה מסודרת, ומחזירה `False` על מנת לאפשר לשגיאה לצוף הלאה לצורך טיפול ברמת האפליקציה.

---

### 🐍 17. גרסת Python נדרשת
* **Python 3.10 ומעלה** (נבדק ותואם ל-Python 3.11–3.14).

---

### ▶️ 18. הוראות הרצה מלאות

1. שכפול המאגר (Clone):
```bash
git clone [https://github.com/orziser-cmd/AirMatrix.git](https://github.com/orziser-cmd/AirMatrix.git)
cd AirMatrix

git log --oneline --graph --all
* 550ed9e (HEAD -> main, origin/main) feat: implement main demonstration runner
* f7d1543 feat: add custom iterators, pipeline and discount context manager
* 7cc6942 feat: implement sorting algorithms with functions and lambdas
* 1b130dc feat: implement TicketRepository streaming generator
* fdcb515 feat: implement Passenger and Ticket class hierarchy
* d664b8f feat: add jsonl flight sample dataset
* e2a6cf3 chore: initialize project root configuration and documentation