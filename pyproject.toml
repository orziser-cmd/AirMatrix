[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "airmatrix"
version = "0.1.0"
description = "AirMatrix Flight & Baggage Management System"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []